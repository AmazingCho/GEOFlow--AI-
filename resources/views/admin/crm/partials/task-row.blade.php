<div class="flex items-start gap-3 px-5 py-4">
    <form method="POST" action="{{ route($task->status === 'done' ? 'admin.crm.tasks.reopen' : 'admin.crm.tasks.complete', ['taskId'=>$task->id]) }}">@csrf<button type="submit" title="{{ $task->status === 'done' ? '重新打开' : '标记完成' }}" class="mt-0.5 flex h-7 w-7 items-center justify-center rounded-full border {{ $task->status === 'done' ? 'border-emerald-300 bg-emerald-50 text-emerald-700' : 'border-gray-300 bg-white text-gray-400 hover:border-emerald-400 hover:text-emerald-600' }}"><i data-lucide="check" class="h-4 w-4"></i></button></form>
    <div class="min-w-0 flex-1"><div class="font-medium {{ $task->status === 'done' ? 'text-gray-400 line-through' : 'text-gray-900' }}">{{ $task->title }}</div><div class="mt-1 flex flex-wrap gap-x-3 gap-y-1 text-xs text-gray-500"><span class="{{ $task->due_at && $task->due_at->isPast() && $task->status === 'open' ? 'font-semibold text-red-600' : '' }}">{{ $task->due_at?->format('Y-m-d H:i') ?: '未设截止时间' }}</span>@if($task->customer)<span>{{ $task->customer->company_name }}</span>@endif @if($task->inquiry)<a class="text-blue-600" href="{{ route('admin.crm.inquiries.show',['inquiryId'=>$task->inquiry_id]) }}">{{ $task->inquiry->subject }}</a>@endif @if($task->opportunity)<a class="text-blue-600" href="{{ route('admin.crm.opportunities.edit',['opportunityId'=>$task->opportunity_id]) }}">{{ $task->opportunity->name }}</a>@endif</div></div>
</div>
