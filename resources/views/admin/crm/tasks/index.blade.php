@extends('admin.layouts.app')
@section('content')
<div class="px-4 sm:px-0"><div class="mb-6"><h1 class="text-2xl font-bold text-gray-900">CRM 待办</h1><p class="mt-1 text-sm text-gray-500">未来要做的事情放这里，已经发生的沟通留在活动记录。</p></div>@include('admin.crm.partials.nav',['currentCrmTab'=>'tasks'])
<form method="GET" class="mb-5 flex flex-wrap gap-3 rounded-lg border border-gray-200 bg-white p-4"><select name="scope" class="rounded-md border border-gray-300 px-3 py-2 text-sm outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500"><option value="mine" @selected($scope==='mine')>我的待办</option><option value="unassigned" @selected($scope==='unassigned')>未分配</option><option value="all" @selected($scope==='all')>全部</option></select><select name="status" class="rounded-md border border-gray-300 px-3 py-2 text-sm outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500"><option value="open" @selected($status==='open')>待处理</option><option value="done" @selected($status==='done')>已完成</option><option value="">全部状态</option></select><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white">筛选</button></form>
<section class="rounded-lg border border-gray-200 bg-white shadow-sm"><div class="divide-y divide-gray-100">@forelse($tasks as $task)@include('admin.crm.partials.task-row',['task'=>$task])@empty<div class="py-14 text-center text-sm text-gray-500">当前没有待办</div>@endforelse</div>@if($tasks->hasPages())<div class="border-t border-gray-100 px-5 py-4">{{ $tasks->links() }}</div>@endif</section></div>
@endsection
